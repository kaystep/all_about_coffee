<% provide(:title, 'All users') %>

<h1 class="text-center">All users</h1>
	
<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
   <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>

	</div>
</div>

