<% provide(:title, 'All users') %>
<div class="row">
	<div class="col-md-offset-5">
		<h1>All users</h1>
	</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
   <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>

	</div>
</div>
